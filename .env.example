# Copy to .env and fill in locally. Never commit a real key.
# Used by batch generation (`npm run batch`). The web app stores
# the Gemini key in browser localStorage via Settings instead.
GEMINI_API_KEY=
