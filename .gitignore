node_modules/

# Secrets and local environment — never commit API keys
.env
.env.*
!.env.example
*.local
*.pem
*.p12
*.key
secrets/
.secrets/
credentials.json
**/service-account*.json

# Improve-run worker scratch — previews and progress.json are the keepers
stories/improve-runs/**/_incoming/
stories/improve-runs/**/*.tmp
